# Netlify site environment variable (set in Netlify UI, not committed with a real value)
BACKEND_ORIGIN=https://narxoz-dissertation-beta-api.onrender.com

# Backend defaults
MAX_UPLOAD_MB=25
RESULT_TTL_MINUTES=15
MAX_CONCURRENT_JOBS=2
